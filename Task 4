# Реализуйте базовый класс Car.
# у класса должны быть следующие атрибуты: speed, color, name, is_police (булево).
# А также методы: go, stop, turn(direction), которые должны сообщать, что машина поехала, остановилась, повернула (куда);
# опишите несколько дочерних классов: TownCar, SportCar, WorkCar, PoliceCar;
# добавьте в базовый класс метод show_speed, который должен показывать текущую скорость автомобиля;
# для классов TownCar и WorkCar переопределите метод show_speed. При значении скорости свыше 60 (TownCar) и 40 (WorkCar) должно выводиться сообщение о превышении скорости.
# Создайте экземпляры классов, передайте значения атрибутов. Выполните доступ к атрибутам, выведите результат. Вызовите методы и покажите результат.

class Car:

    def __init__(self, name, speed, color, is_police=False):
        self.name = name
        self.speed = speed
        self.color = color
        self.is_police = is_police

    def go(self):
        return f'Автомобиль {self.name} начал движение!'

    def stop(self):
        return f'\n{self.name} была остановлена.'

    def turn(self, direction):
        return f'\n{self.name} повернула на {direction}.'

    def show_speed(self):
        return f'\nСкорость автомобиля {self.speed} км/ч'

class TownCar(Car):
    def show_speed(self):
        if self.speed > 60:
            return f'\nСкорость автомобиля выше допустимой! Текущая скорость: {self.speed} км/ч'
        else:
            return f'Скорость автомобиля {self.name} в пределах нормы'

class SportCar(Car):
    pass

class WorkCar(Car):
    def show_speed(self):
        if self.speed > 40:
            return f'\nСкорость автомобиля выше допустимой! Текущая скорость: {self.speed} км/ч'
        else:
            return f'Скорость автомобиля {self.name} в пределах нормы'

class PoliceCar(Car):
    pass

town = TownCar('Lada Vesta', 90, 'blue', False)
print('Автомобиль №1:\n' + town.go(), town.show_speed(), town.turn('лево'), town.turn('право'), town.stop(), ('\n'))

sport = SportCar('Audi RS6 Avant', 40, 'red', False)
print('Автомобиль №2:\n' + sport.go(), sport.show_speed(), sport.turn('лево'), sport.stop(), ('\n'))

work = WorkCar('BMW M5', 100, 'red', False)
print('Автомобиль №3:\n' + work.go(), work.show_speed(), work.turn('право'), work.turn('лево'), work.stop(), ('\n'))

police = PoliceCar('BRABUS 900', 60, 'yellow', True)
print('Автомобиль №4:\n' + police.go(), police.show_speed(), police.turn('право'), police.stop())

