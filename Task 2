# 2. Для списка реализовать обмен значений соседних элементов. 
# Значениями обмениваются элементы с индексами 0 и 1, 2 и 3 и т. д. 
# При нечётном количестве элементов последний сохранить на своём месте. 
# Для заполнения списка элементов нужно использовать функцию input().

q = int(input("Сколько товаров в список Вы желаете добавить?\n\t Укажите кол-во товаров: "))
my_lst = []
for i in range(q):
    my_lst.append(input(f"Товар # {i + 1} : "))
print(f"Ваши товары из списка:\n{my_lst}")
for x in range(0, (len(my_lst) - 1),2):
    my_lst[x], my_lst[x+1] = my_lst[x+1], my_lst[x]
print(f"Отредактированный список ваших товаров:\n{my_lst}")
